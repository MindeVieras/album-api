
import { getUserSettings } from '../models/utils'
const Auth = require('../helpers/authenticate')

module.exports = function(app) {

  // app.get('/api/utils/get-user-settings/:id', Auth.isAuthed, getUserSettings)

}
